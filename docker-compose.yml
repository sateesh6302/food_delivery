version: "3.8"
services:
 frontend:
  build: ./frontend
  container_name: food-frontend
  ports:
   - "5173:5173"
  depends_on:
   - backend
 backend:
  build: ./backend
  container_name: food-backend
  ports:
   - "4000:4000"
  env_file:
   - ./backend/.env